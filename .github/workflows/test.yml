name: test me

on:
  # Webhook Event
  push:
    branches:
      - main
      - feat/*
  pull_request:
    branches:
      - main
  issues:
    types:
      - opened
      - closed
  # Schedule Event
  # Manual event

jobs:
  Echo:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo Hello World
